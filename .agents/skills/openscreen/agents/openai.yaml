interface:
  display_name: "Openscreen"
  short_description: "Repo-specific patterns and workflows for openscreen"
  default_prompt: "Use the openscreen repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true